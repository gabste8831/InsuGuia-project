name: insuguia_mobile
description: "A new Flutter project."
publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: '>=3.3.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Nossos pacotes (O APP PRECISA DELES PARA RODAR)
  provider: ^6.1.2
  sqflite: ^2.3.3+1
  path_provider: ^2.1.3
  google_fonts: ^6.2.1 
  intl: ^0.19.0  # <--- MUDOU PARA CÁ (IMPORTANTE!)

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  # intl estava aqui errado

flutter:
  uses-material-design: true